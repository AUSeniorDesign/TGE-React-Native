import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1
    },
    button: {
        color: '#157dfa',
        fontWeight: 'bold',
        fontSize: 16
    }
})